{
  "name": "bedrock-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for AWS Bedrock",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
